package;

import bonsai.Engine;
import bonsai.scene.Scene;

class Main {
	public static function main() {
		var engine = new Engine();
		engine.start();
		//engine.currentScene.CreateNewScene();
	}
}
